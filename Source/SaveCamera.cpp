#include <SaveCamera.h>

#include <sstream>
#include <fstream>

#include <Path.h>

#define NAME(X) #X

bool SaveCamera::Save(const char* SlotName)
{
    return true;
}

bool SaveCamera::Load(const char* SlotName)
{
    
    return true;
}
